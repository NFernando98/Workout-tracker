{
    "version": 2,
    "rewrites": [
        {
         "src": "/{.*)",
         "dest": "/"
        }
    ]
    "builds": [
      {
        "src": "backend/server.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "backend/server.ts"
      }
    ]
  }
  
